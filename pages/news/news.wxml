<view class="main">
    <view class="wrp" wx:for="{{news_list}}" wx:key="{{index}}">
        <view  class="title"   url="detail/detail?id={{item.url}}">
            {{item.title}}
         </view>
    </view>
    

</view>